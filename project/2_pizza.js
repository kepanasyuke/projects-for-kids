// ЗАДАНИЕ 2: множество. Замени заглушки своими продуктами.
// Пример: const ingredients = new Set(['сыр', 'томаты', 'сыр', 'оливки']);
export const ingredients = new Set(['ВПИШИ_ПРОДУКТ_1', 'ВПИШИ_ПРОДУКТ_2', 'ВПИШИ_ПРОДУКТ_3', 'ВПИШИ_ПРОДУКТ_4', 'ВПИШИ_ПРОДУКТ_5']);
const template = new Set(['ВПИШИ_ПРОДУКТ_1', 'ВПИШИ_ПРОДУКТ_2', 'ВПИШИ_ПРОДУКТ_3', 'ВПИШИ_ПРОДУКТ_4', 'ВПИШИ_ПРОДУКТ_5']);
export const pizzaTask = { title: 'Пицца · множество', hint: 'Впиши продукты между круглыми скобками Set.', example: "new Set(['сыр', 'томаты', 'сыр', 'оливки'])", valid: () => ingredients.size > 0 && [...ingredients].every(item => item && !item.startsWith('ВПИШИ_')) && [...ingredients].join() !== [...template].join(), draw(ctx, frame, width, height) { const centerX = width / 2, centerY = height / 2 + 20; ctx.save(); ctx.translate(centerX, centerY); ctx.rotate(frame * .004); ctx.fillStyle = '#d89a55'; ctx.beginPath(); ctx.ellipse(0, 0, 210, 65, 0, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = '#c74638'; ctx.beginPath(); ctx.ellipse(0, -8, 195, 57, 0, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = '#ffd166'; ctx.beginPath(); ctx.ellipse(0, -14, 184, 49, 0, 0, Math.PI * 2); ctx.fill(); [...ingredients].forEach((item, index) => { const angle = index * 2.4 + frame * .006, radius = 75 + (index % 2) * 55; ctx.fillStyle = ['#ef5b45', '#72b35a', '#fff0b5', '#9b6b4f', '#e19b42'][index % 5]; ctx.beginPath(); ctx.arc(Math.cos(angle) * radius, -25 + Math.sin(angle) * 27, 15, 0, Math.PI * 2); ctx.fill(); }); ctx.restore(); } };
