// ЗАДАНИЕ 3: связный список. Заполни вагоны и вставь новый вагон.
// Пример: const trainCars = ['Паровоз', 'Вагон 1']; trainCars.splice(1, 0, 'Вагон-лаборатория');
export const trainCars = ['ВПИШИ_ПАРОВОЗ', 'ВПИШИ_ВАГОН_1', 'ВПИШИ_ВАГОН_2', 'ВПИШИ_ВАГОН_3'];
trainCars.splice(2, 0, 'ВПИШИ_НОВЫЙ_ВАГОН');
const template = ['ВПИШИ_ПАРОВОЗ', 'ВПИШИ_ВАГОН_1', 'ВПИШИ_НОВЫЙ_ВАГОН', 'ВПИШИ_ВАГОН_2', 'ВПИШИ_ВАГОН_3'];
export const trainTask = { title: 'Поезд · связный список', hint: 'Назови вагоны и вставь новый через splice.', example: "trainCars.splice(2, 0, 'Вагон-лаборатория')", valid: () => trainCars.length === 5 && trainCars.join() !== template.join() && trainCars.every(car => car && !car.startsWith('ВПИШИ_')), draw(ctx, frame, width, height) { const start = width / 2 - (trainCars.length - 1) * 72; trainCars.forEach((car, index) => { const x = start + index * 144 + (frame * .7 % 144); const y = height / 2 + Math.sin(frame * .04 + index) * 5; ctx.fillStyle = index === 0 ? '#ef5b45' : ['#54e8e8', '#ffd166', '#9b8cff', '#69e6a5'][index % 4]; ctx.fillRect(x - 55, y - 35, 110, 70); ctx.fillStyle = '#18202a'; ctx.fillRect(x - 32, y - 18, 24, 20); ctx.fillRect(x + 8, y - 18, 24, 20); ctx.fillStyle = '#111820'; ctx.beginPath(); ctx.arc(x - 32, y + 40, 11, 0, Math.PI * 2); ctx.arc(x + 32, y + 40, 11, 0, Math.PI * 2); ctx.fill(); if (index < trainCars.length - 1) { ctx.strokeStyle = '#f4f1e8'; ctx.lineWidth = 5; ctx.beginPath(); ctx.moveTo(x + 55, y); ctx.lineTo(x + 89, y); ctx.stroke(); } }); } };
