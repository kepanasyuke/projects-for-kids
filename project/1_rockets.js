// ЗАДАНИЕ 1: массив. Замени все 5 заглушек своими названиями ракет.
// Пример: const rockets = ['Молния', 'Комета', 'Звезда', 'Сатурн', 'Феникс'];
export const rockets = ['ВПИШИ_РАКЕТУ_1', 'ВПИШИ_РАКЕТУ_2', 'ВПИШИ_РАКЕТУ_3', 'ВПИШИ_РАКЕТУ_4', 'ВПИШИ_РАКЕТУ_5'];
const template = ['ВПИШИ_РАКЕТУ_1', 'ВПИШИ_РАКЕТУ_2', 'ВПИШИ_РАКЕТУ_3', 'ВПИШИ_РАКЕТУ_4', 'ВПИШИ_РАКЕТУ_5'];
export const rocketsTask = { title: 'Ракеты · массив', hint: 'Впиши 5 названий ракет в квадратных скобках.', example: "['Молния', 'Комета', 'Звезда', 'Сатурн', 'Феникс']", valid: () => rockets.length === 5 && rockets !== template && rockets.every(name => name && !name.startsWith('ВПИШИ_')), draw(ctx, frame, width, height) { ctx.fillStyle = '#8fe8ff'; rockets.forEach((name, index) => { const angle = frame * .012 + index * Math.PI * 2 / rockets.length; const x = width / 2 + Math.cos(angle) * (150 + index * 35); const y = height / 2 + Math.sin(angle) * (80 + index * 18); ctx.save(); ctx.translate(x, y); ctx.rotate(angle + Math.PI / 2); ctx.fillStyle = ['#ff647c', '#ffd166', '#69e6a5', '#8fe8ff', '#c7a6ff'][index]; ctx.beginPath(); ctx.moveTo(0, -28); ctx.lineTo(11, 9); ctx.lineTo(0, 5); ctx.lineTo(-11, 9); ctx.closePath(); ctx.fill(); ctx.fillStyle = '#ff8b42'; ctx.fillRect(-4, 10, 8, 20 + Math.sin(frame * .08 + index) * 8); ctx.restore(); }); } };
